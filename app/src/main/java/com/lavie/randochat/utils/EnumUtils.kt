package com.lavie.randochat.utils

enum class ChatType {
    RANDOM,
    AGE,
    LOCATION
}

enum class MessageType {
    TEXT,
    IMAGE,
    VOICE
}

enum class SplashType {
    LOGIN_CHECK,
    MATCHING
}